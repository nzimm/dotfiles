#
# ~/.profile
#
#

[[ -z $DISPLAY ]] && [[ "$(tty)" = '/dev/tty1' ]] && exec startx i3; exit

#[[ "$XDG_CURRENT_DESKTOP" == "KDE" ]] || export QT_QPA_PLATFORMTHEME="qt5ct"

#[[ -f ~/.extend.profile ]] && . ~/.extend.profile
